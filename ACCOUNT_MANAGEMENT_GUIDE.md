# 账户管理功能使用指南

## 功能概述

为邮箱登录用户提供完整的账户管理功能，包括：
- 修改密码
- 编辑个人资料（昵称）
- 查看账户信息
- 退出登录

## 功能入口

### 1. 个人中心页面头部
- 点击头部的用户头像和昵称区域
- 显示用户登录状态（✅ 已登录）
- 未登录用户点击可跳转到登录页面

### 2. 账户管理卡片
在个人中心页面中间部分，登录后会显示"账户管理"卡片：
- **个人资料**: 修改昵称、头像等
- **账户安全**: 修改密码、安全设置

## 功能详情

### 📝 修改昵称
1. 进入账户设置页面
2. 点击"修改昵称"
3. 输入新的昵称
4. 点击"保存"
5. 昵称会立即在个人中心头部更新

### 🔒 修改密码
1. 进入账户设置页面
2. 点击"修改密码"
3. 输入当前密码（注：Supabase 实际不验证旧密码）
4. 输入新密码（至少6个字符）
5. 再次输入新密码确认
6. 点击"确认修改"
7. 密码修改成功

**安全提示**：
- 新密码至少需要6个字符
- 两次输入的新密码必须一致
- 定期修改密码以保护账户安全

### 👤 查看账户信息
账户设置页面显示：
- 用户头像（首字母缩写）
- 用户昵称
- 邮箱地址（已验证标记）

### 🚪 退出登录
1. 在账户设置页面底部
2. 点击红色的"退出登录"按钮
3. 确认退出
4. 自动返回登录页面

## 技术实现

### 文件结构
```
lib/
├── account_settings_page.dart  # 账户设置页面（新增）
├── profile_screen.dart          # 个人中心页面（已更新）
└── email_login_page.dart        # 邮箱登录页面
```

### Supabase 集成
使用 Supabase Auth API：
- `auth.currentUser` - 获取当前登录用户
- `auth.updateUser()` - 更新用户信息（昵称、密码）
- `auth.signOut()` - 退出登录

### 用户元数据
昵称存储在 `user.userMetadata['name']` 中

### 状态管理
- 使用 `StatefulWidget` 管理页面状态
- 实时同步用户信息
- Loading 状态提示

## 用户体验优化

### 🎨 视觉设计
- 圆形头像显示用户昵称首字母
- 品牌色主题一致
- 多层阴影卡片设计
- 清晰的信息层级

### ✨ 交互反馈
- 表单验证（密码长度、一致性）
- 操作成功/失败提示
- 加载状态动画
- 确认对话框（退出登录、删除等敏感操作）

### 🔐 安全保护
- 密码输入框隐藏显示
- 敏感操作二次确认
- 邮箱不可修改（已验证标记）

## 使用流程示例

### 完整流程：邮箱登录 → 修改资料 → 退出
1. 用户通过邮箱和密码登录
2. 进入首页，底部导航切换到"我的"
3. 点击个人中心头部的头像区域
4. 进入账户设置页面
5. 修改昵称为"铲屎官小王"
6. 修改密码以提高安全性
7. 查看账户信息确认修改成功
8. 点击"退出登录"按钮
9. 返回登录页面

## 未来规划

### 待开发功能
- [ ] 头像上传和更换
- [ ] 手机号绑定和修改
- [ ] 账户注销
- [ ] 第三方账号绑定（微信、QQ等）
- [ ] 登录历史记录
- [ ] 安全日志

### 优化方向
- [ ] 密码强度检测和提示
- [ ] 邮箱验证和更换
- [ ] 双因素认证（2FA）
- [ ] 生物识别登录（指纹、Face ID）

## 测试建议

1. **功能测试**
   - 登录后修改昵称
   - 修改密码并重新登录验证
   - 退出登录并重新登录
   
2. **边界测试**
   - 输入空昵称
   - 密码少于6个字符
   - 两次密码输入不一致
   
3. **用户体验测试**
   - 未登录用户不显示账户管理卡片
   - 各种成功/失败提示是否清晰
   - 页面跳转是否流畅

---

**更新日期**: 2025-11-16  
**版本**: v1.0
