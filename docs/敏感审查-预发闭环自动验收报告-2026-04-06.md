# 敏感审查预发闭环自动验收报告（2026-04-06）

## 1. 执行范围

- 环境：`tcftpcvcldfudzxgemdh`（Supabase 预发）
- 模式：不接入第三方 provider，按 `local_fallback` 验收框架闭环
- 执行方式：MCP + 终端自动化

## 2. 前置门禁修复结果

- 已部署 `avatar-finalize`（`verify_jwt=true`）。
- 已应用清理迁移：`moderation_temp_bucket_cleanup_v2`。
- 已确认前端代码不直接调用旧 `diary` slug（调用的是 `diary-v3`）。
- 已重新发布：
  - `moderate-text` -> version 2
  - `moderate-image` -> version 2

## 3. 清理机制验证（pg_cron 不可用替代）

- 检测到 `pg_cron` 未安装（`pg_extension` 查询为空）。
- SQL 直接删除 `storage.objects` 会被保护机制拦截（预期行为）。
- 已部署替代函数 `cleanup-moderation-temp`（`verify_jwt=true`，走 Storage API 删除）。
- dry-run 验证通过：`ok=true`，`scanned=0`，`candidates=0`，`deleted=0`。

## 4. 自动化验收结果

### 4.1 安全基线

- `moderate-text` 无 token：401（通过）
- `moderate-image` 伪造 token：401（通过）
- `avatar-finalize` 无 token：401（通过）
- `moderate-text` 使用 anon token：401 + `MODERATION_UNAUTHORIZED`（通过）
- `moderate-image` 使用 anon token 且伪造路径：401 + `MODERATION_UNAUTHORIZED`（通过）

### 4.2 触发器防绕过

通过 SQL 块验证以下触发器对敏感词更新进行阻断（通过）：
- `users_profiles.nickname`
- `pets.name`
- `pet_diaries.content`

### 4.3 可观测性与 trace

- `moderation_logs`、`moderation_errors` 表存在且可写（通过）。
- 通过验收探针写入并关联同一 `trace_id`，双表 join 成功（通过）。
- 边缘函数日志可见本次测试请求（通过）。

## 5. 风险项与门禁结论

## 5.1 风险项

- 旧函数 `diary` 仍在线且 `verify_jwt=false`，虽前端未直接调用，但存在潜在绕过面。
- 当前自动化测试未覆盖“真实用户 token 下的头像 temp->formal 全链路成功流”与“image_output 阻断写 formal”的完整业务流。
- `pg_cron` 不可用，需依赖外部调度触发清理函数。

## 5.2 门禁结论

- 结论：**有条件可进入小流量灰度**（先批次 1），但需先完成“人工必做项”。

## 6. 人工必做项（上线前）

1. 使用真实测试账号完成以下端到端联调并回填模板：
   - 头像：`avatars-temp -> avatar-finalize -> avatars`
   - 生图：`ai-images-temp -> img-gen-start/check-v2 -> ai-images`
2. 对旧 `diary` 函数进行下线或入口隔离（至少在网关层封禁外部调用）。
3. 配置外部调度（CI/任务平台）定时调用 `cleanup-moderation-temp`。
4. 在 `docs/敏感审查-联调与验收记录模板.md` 中补齐实际结果、P95 与放量结论。

