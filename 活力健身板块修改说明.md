活力健身板块修改说明

训练页面 (partner_fit_training_page.dart)

倒计时显示优化
- 倒计时文字使用等宽字体(monospace)，避免冒号左右抖动
- 时间小于100秒时仅显示秒数，大于等于100秒显示MM:SS格式

实际运动时长
- 新增正计时功能，从训练开始持续计时
- 显示在倒计时圆圈内，格式为"X分X秒"或"X秒"
- 完成时训练时长使用实际运动时长，而非计划时长

嘀嗒音效
- 倒计时每秒播放系统提示音(SystemSound.click)和触觉反馈
- 新增音效开关按钮(仅显示喇叭图标，无文字)
- 音效设置保存到SharedPreferences，下次训练自动恢复

鼓励文字
- 动作完成时显示鼓励文字(加油、坚持、很棒等)
- 使用Keep风格的橙色主题和淡入淡出动画
- 显示在动作名称下方

致敬元素
- 页面底部添加"Inspired by Keep"小标签

完成页面 (partner_fit_completion_page.dart)

训练时长显示
- 使用实际运动时长替代计划时长
- 从FitnessRecord的durationMinutes字段读取

Keep风格元素
- 添加"自律给我自由"鼓励文案(Keep橙色)
- 页面底部添加"Inspired by Keep"标签

技术实现
- 实际运动时长：使用Timer.periodic每秒递增actualDurationSeconds
- 音效播放：SystemSound.play(SystemSoundType.click) + HapticFeedback.selectionClick()
- 音效设置：SharedPreferences存储_tickSoundEnabled状态
- 时间格式化：_formatTime()函数处理小于100秒的特殊显示
