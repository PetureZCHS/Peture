# LeanCloud 短信验证码问题说明

## 问题原因

你看到"发送验证码失败，请稍后重试"的错误，这是因为：

**LeanCloud 需要配置短信签名才能发送真实的短信验证码。**

根据 LeanCloud 的安全规范，所有短信服务都必须：
1. 配置并审核通过短信签名
2. 配置短信模板（可使用默认模板）
3. 账户充值（短信服务按量计费）

## 错误详情

从控制台日志可以看到：
```
{"code":1,"error":"签名无效，应用至少需要一个默认签名，或签名未通过审核"}
```

这是 LeanCloud 的正常安全机制，不是代码问题。

## 解决方案

### 方案一：使用测试号（推荐用于开发测试）

我已经更新了代码，对测试号 `18639529172` 进行特殊处理：

1. **点击"获取验证码"按钮** - 不会实际调用 API，直接显示成功
2. **输入验证码** `746018`
3. **点击"登录"** - 使用模拟登录，创建本地会话

**测试步骤：**
```
手机号：18639529172
验证码：746018（固定）
```

### 方案二：配置 LeanCloud 短信服务（用于正式上线）

#### 步骤 1：登录 LeanCloud 控制台
访问：https://console.leancloud.cn/

#### 步骤 2：配置短信签名
1. 进入你的应用
2. 点击左侧菜单 **设置 > 短信**
3. 点击 **短信签名**
4. 点击 **创建签名**
5. 填写签名信息：
   - 签名名称：例如 "智宠合生"
   - 签名类型：选择"网站/应用"
   - 应用名称：智宠合生
   - 其他证明材料：上传应用截图或说明
6. 提交审核（通常 1-2 个工作日）

#### 步骤 3：配置短信模板（可选）
LeanCloud 提供了默认的验证码模板，一般不需要自定义。

如果需要自定义：
1. 点击 **短信模板**
2. 点击 **创建模板**
3. 填写模板内容，例如：
   ```
   您的验证码是 {{code}}，5分钟内有效。
   ```

#### 步骤 4：账户充值
1. 点击左上角账户名
2. 选择 **财务 > 充值**
3. 充值金额（短信约 0.05 元/条）

#### 步骤 5：更新代码（审核通过后）
签名审核通过后，需要在请求中添加签名参数：

```dart
body: jsonEncode({
  'mobilePhoneNumber': mobilePhoneNumber,
  'sign': '智宠合生', // 你的短信签名
}),
```

## 当前代码更新

我已经修改了 `lib/config/leancloud_service.dart`：

### 更新内容：
1. **测试号特殊处理**：手机号 `18639529172` 不会实际发送短信
2. **模拟登录**：使用测试号 + 验证码 `746018` 可以直接登录
3. **本地会话**：创建模拟的 sessionToken 和用户信息

### 使用方法：
```dart
// 测试登录流程
手机号：18639529172
点击"获取验证码" ✓
输入验证码：746018
勾选协议 ✓
点击"登录" ✓
```

## 代码对比

### 修改前（会调用 API）：
```dart
static Future<bool> requestSmsCode(String mobilePhoneNumber) async {
  final url = Uri.parse('${LeanCloudConfig.serverUrl}/1.1/requestSmsCode');
  // ... 直接调用 API
}
```

### 修改后（测试号跳过 API）：
```dart
static Future<bool> requestSmsCode(String mobilePhoneNumber) async {
  // 测试号直接返回成功
  if (mobilePhoneNumber == '18639529172') {
    print('测试号，跳过实际发送验证码');
    return true;
  }
  // 其他号码才调用 API
}
```

## 验证流程图

```
开发测试阶段：
用户输入 18639529172
    ↓
点击获取验证码
    ↓
代码检测到是测试号，直接返回成功（不调用 API）
    ↓
用户输入 746018
    ↓
点击登录
    ↓
代码模拟登录成功，创建本地会话
    ↓
跳转到主界面 ✓

正式上线阶段：
用户输入真实手机号
    ↓
点击获取验证码
    ↓
调用 LeanCloud API（需要配置短信签名）
    ↓
用户收到短信验证码
    ↓
用户输入验证码
    ↓
调用 LeanCloud 登录 API
    ↓
登录成功 ✓
```

## 现在可以做什么

✅ **立即可用**：使用测试号 `18639529172` + 验证码 `746018` 测试完整登录流程

✅ **开发阶段**：继续开发其他功能，短信服务暂时使用模拟方式

❌ **暂时不可用**：真实手机号无法接收验证码（需要配置签名）

## 下一步建议

1. **现在**：使用测试号完成功能开发和测试
2. **准备上线前**：
   - 配置 LeanCloud 短信签名
   - 等待审核通过（1-2个工作日）
   - 账户充值
   - 移除测试号的特殊处理代码
   - 添加短信签名参数

## 相关文档

- [LeanCloud 短信服务文档](https://docs.leancloud.cn/sdk/sms/guide/)
- [短信签名配置指南](https://docs.leancloud.cn/sdk/sms/guide/#%E7%AD%BE%E5%90%8D)
- [短信模板配置](https://docs.leancloud.cn/sdk/sms/guide/#%E6%A8%A1%E6%9D%BF)

## 常见问题

**Q: 为什么不能发送真实验证码？**
A: LeanCloud 的安全要求，防止短信滥用。必须先配置签名。

**Q: 测试阶段怎么办？**
A: 使用测试号 18639529172 和固定验证码 746018。

**Q: 签名审核要多久？**
A: 通常 1-2 个工作日。

**Q: 需要花钱吗？**
A: 短信服务按量计费，约 0.05 元/条，需要先充值。

**Q: 可以免费使用吗？**
A: LeanCloud 每月有免费额度（具体查看你的套餐），超出部分收费。
